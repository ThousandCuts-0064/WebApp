const config = {
    server: "localhost",
    user: "admin",
    password: "admin",
    database: "WebAppDB",
    options: {
        encrypt: true,
        trustServerCertificate: true,
    },
};

export default config;
